def terminal_lingo():
   command = input("Type a command for a definition: ")
   if (command == "touch"):
      print("\n")
      print("This command allows you to create text files")
   if (command == "cat"):
      print("\n")
      print("This command allows you to concatenate files")
   if (command == "cd"):
      print("\n")
      print("This command  changes directory")
   if (command == "Is")
      print("\n")
      print("This command lists folders")
   if (command == "mkdir"):
      print("\n")
      print("This command can create folders")
terminal_lingo()
